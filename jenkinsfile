pipeline {
    agent {
        node{
            label 'maven'
        }
    }
   
    stages {
        stage('build code') {
            steps {
                withEnv("/opt/apache-maven-3.9.8/bin:$PATH") {
                    sh 'mvn clean deploy'
                }
            }
        }
    }
}
